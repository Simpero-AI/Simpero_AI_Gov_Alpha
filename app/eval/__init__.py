"""L2 evaluation instruments (SIM-351). Pure, corpus-agnostic metric code shared
by the CI runner (synthetic golden set, sparse-only) and the L3 real-CIM runner
(SIM-357, dense leg on staging). No DB or I/O lives here."""
